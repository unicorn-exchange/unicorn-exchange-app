{
  "name": "unicorn-exchange-app",
  "license": "ISC",
  "scripts": {
    "postinstall": "run-p install:backend install:frontend install:types",
    "install:backend": "cd unicorn-exchange-backend && yarn",
    "install:frontend": "cd unicorn-exchange-frontend && yarn",
    "install:types": "cd unicorn-exchange-types && yarn"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
