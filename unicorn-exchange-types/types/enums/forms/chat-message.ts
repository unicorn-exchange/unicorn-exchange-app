export enum chatMessageFields {
  text = "text",
  fromUserId = "fromUserId",
  chatId = "chatId",
  status = "status",
  fontType = "fontType",
  attachmentUrl = "attachmentUrl",
}

export enum MessageStatuses {
  New,
  Read,
  Deleted,
}

export enum MessageFontTypes {
  Normal,
  Bold,
  Italic,
}
